POSTGRES_DB=aiworker
POSTGRES_USER=aiworker
POSTGRES_PASSWORD=change_me_strong_password
ADMIN_API_KEY=change_me_admin_key

# used by golang-migrate
DATABASE_URL=postgres://aiworker:change_me_strong_password@localhost:5433/aiworker?sslmode=disable

# Dashboard (baked into the React bundle at build time — must match the Tailscale IP of the VPS)
VITE_API_URL=http://100.106.192.45:8081
