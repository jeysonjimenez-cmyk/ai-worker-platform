[project]
name = "agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "nvidia-ml-py>=12.0.0",
    "psutil>=6.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.15.0",
]

[tool.ruff]
line-length = 100
